[pytest]
testpaths = tests
# Put the backend package root on sys.path so `app` imports resolve regardless of how pytest
# is invoked. `python -m pytest` adds the CWD implicitly, but the `pytest` console script
# does not — which is how CI runs it.
pythonpath = .
